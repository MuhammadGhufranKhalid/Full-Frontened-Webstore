import ResourceTable from "../../components/admin/ResourceTable";import { usersService } from "../../api/usersService";
export default function UsersAdminPage(){return <ResourceTable title="Users" description="Manage frontend user records." load={async()=>{const r=await usersService.getAll();return r.users.map(u=>({...u,name:`${u.firstName} ${u.lastName}`}));}} createItem={usersService.create} updateItem={usersService.update} deleteItem={usersService.remove} emptyItem={{firstName:"",lastName:"",email:"",username:"",role:"user"}} columns={[{key:"firstName",label:"First name"},{key:"lastName",label:"Last name"},{key:"email",label:"Email"},{key:"username",label:"Username"},{key:"role",label:"Role"}]}/>}
